import styled from 'styled-components';

export const FileTreeContainer = styled.div`
    display: flex;
    flex-grow: 1;
    flex-direction: column;
    max-height: 100%;
    overflow: auto;
`;
export const FileTreeInner = styled.div`
    padding: 16px;
`;
